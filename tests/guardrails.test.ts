/**
 * guardrails.test.ts — Tests for guardrails G-1 through G-5.
 *
 * Each guardrail has a dedicated test asserting correct behavior,
 * independent of the Classifier or any LLM call. (R-6, AC-2)
 *
 * Implementation: Day 2 (T2.3)
 */

import { describe, it, expect } from "vitest";

describe("Guardrails", () => {
  // TODO: T2.3 — write tests for G-1 through G-5
  it.todo("should be implemented in Day 2");
});
