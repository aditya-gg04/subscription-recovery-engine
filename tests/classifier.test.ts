/**
 * classifier.test.ts — Tests for the Classifier module.
 *
 * Covers: deterministic lookup path, LLM fallback routing (R-3).
 *
 * Implementation: Day 3+ (T3.1, T4.2)
 */

import { describe, it, expect } from "vitest";

describe("Classifier", () => {
  // TODO: write classifier tests
  it.todo("should be implemented in Day 3");
});
