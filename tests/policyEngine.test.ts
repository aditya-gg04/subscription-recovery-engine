/**
 * policyEngine.test.ts — Tests for the Policy Engine base rule table.
 *
 * Covers: Section 7 rules, R-5 evaluation order (OPT_OUT → guardrails → base rules).
 * Uses hand-constructed Diagnosis/event fixtures — no Classifier or LLM dependency.
 *
 * Implementation: Day 2 (T2.4)
 */

import { describe, it, expect } from "vitest";

describe("Policy Engine", () => {
  // TODO: T2.4 — write tests for each rule in Section 7
  it.todo("should be implemented in Day 2");
});
