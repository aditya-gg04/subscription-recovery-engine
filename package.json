{
  "name": "recovery-agent",
  "version": "1.0.0",
  "description": "Failed-payment / subscription revenue recovery agent for Razorpay",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-data": "tsx generate-synthetic-events.ts",
    "run-batch": "tsx src/runBatch.ts",
    "dashboard:dev": "cd dashboard && npm run dev"
  },
  "dependencies": {
    "zod": "^3.23.0",
    "js-yaml": "^4.1.0",
    "razorpay": "^2.9.0",
    "@google/generative-ai": "^0.21.0",
    "groq-sdk": "^0.8.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "tsx": "^4.16.0",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.0.0"
  }
}
